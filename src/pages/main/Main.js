import React from 'react';
import CameraComponent from './CameraComponent';

const Main = () => {
    return (
        <div>
            <CameraComponent start={true} />
        </div>
    );
};

export default Main;
